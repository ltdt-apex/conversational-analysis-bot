# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
.uv-cache/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Env / secrets
.env
.env.local
.env.*.local

# Data artifacts.
# Note: data/processed.db and data/chroma/ ARE committed so reviewers can
# skip the ~5-minute preprocess step on first run. Only transient SQLite
# WAL / journal files and checkpoint scratch are excluded.
data/processed.db-journal
data/processed.db-wal
data/processed.db-shm
data/checkpoints/
data/topic_taxonomy.yaml.bak

# Eval outputs
eval/results/

# Report build artifacts (the final .docx IS committed under report/)
report/build/

# Office lock files
.~lock.*

# IDE / OS
.vscode/
.idea/
.DS_Store
*.swp
